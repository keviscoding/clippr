// Clippr runtime config.
// Replace these values with the URL + anon key from your Supabase project:
//   Supabase dashboard -> Project Settings -> API -> Project URL + anon public key.
// Anon key is intentionally public-safe; data is protected by Row Level Security.
window.CLIPPR_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: "",
};
